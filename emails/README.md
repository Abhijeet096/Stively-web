# emails

React Email templates for Resend, rendered server-side when transactional emails are sent (enrollment confirmations, contact form receipts, magic-link sign-in emails, etc.).

Nothing here yet - add templates as the flows that need them get built (Step 3+).

# Architecture Decisions

The permanent engineering handbook for Stively. Every major architectural decision — a real trade-off, not a routine implementation detail — gets recorded here before or as it's built. Numbered sequentially, never renumbered or deleted; a reversed decision gets a new entry that supersedes the old one, with a note added to the original.

---

## AD-001: Lead / SalesLead / OfferingRequest stay separate models

**Date:** 2026-08-03
**Problem:** Three models exist for "someone wants to talk to us" — `Lead` (inbound marketing/contact-form capture), `SalesLead` (outbound B2B sales pipeline), `OfferingRequest` (self-service catalog wizard). Surfaced as a suspected duplication during a full-platform architecture audit.
**Options considered:**
1. Merge all three into one `Lead` model with a `source`/`type` discriminator.
2. Keep separate, add/verify explicit bridges between them.
**Chosen solution:** Keep separate (option 2). Bridges already exist both directions: `OfferingRequest.promotedSalesLeadId`, `Business.promotedSalesLeadId`.
**Reason:** The three represent genuinely different shapes and lifecycles — inbound capture has no pricing/commission concept, `SalesLead` carries GST/installments/commission fields inbound capture will never need, `OfferingRequest` is scoped to a specific catalog `Offering` neither of the others are. Forcing one shape onto all three would mean nullable fields relevant to only one-third of rows, and a single status enum trying to represent three different pipelines.
**Trade-offs:** Three models to reason about instead of one; a developer new to the codebase has to learn which one applies where. Mitigated by thorough doc comments on each model explaining its scope and cross-referencing the others.
**Database impact:** None (no change) — this decision is a confirmation of existing schema, not a migration.
**API impact:** None.
**Future considerations:** If a fourth "someone wants to talk to us" shape is ever proposed, re-run this same test before creating a new model: does it have a materially different lifecycle/fields, or is it actually one of these three with a different entry point?
**Related components:** `prisma/schema.prisma` (`Lead`, `SalesLead`, `OfferingRequest`), `src/features/offering-requests/`, `src/features/sales-crm/`

---

## AD-002: Document generation — dynamic per-client PDFs only where per-client data actually varies

**Date:** 2026-08-03
**Problem:** The CEO's client-journey brief proposed ~25 document types (invoice, NDA, service agreement, welcome kit, company profile, handover doc, warranty certificate, training guide, maintenance brochure, etc.). Building all 25 as platform-generated PDFs would be a large, mostly-wasted investment.
**Options considered:**
1. Generate every document dynamically from the platform.
2. Design every document once in Canva, upload manually per client.
3. Split by whether per-client data actually varies.
**Chosen solution:** Option 3. A document is worth dynamic generation only if its content meaningfully differs per client/project (name, dates, amounts, project specifics). Otherwise it's designed once and reused via the existing manual-upload `ClientDocument` flow.
- **Dynamic (build):** Invoice, Payment Receipt, NDA, Service Agreement, Completion Certificate, Warranty Certificate, Welcome Kit (has real per-client merge fields: client name, PM, project ID, dates — corrected from an earlier pass that mis-scoped this as static).
- **Static/Canva, uploaded once (no new code):** Company Profile, Communication Guide, Training Guide, Maintenance Brochure, Referral Brochure.
- **Neither — real portal workflow, not a document at all:** Requirement Gathering Form (client fills in-portal, status Pending/Submitted/Approved), QA Report / UAT Checklist (interactive, client ticks off and approves), Change Request Form (real approval + repricing flow).
- **Skip:** Project Information Sheet (redundant with data already live on the Client Workspace dashboard header).
**Reason:** The marginal cost of a new dynamic-PDF template is low once the rendering engine exists (AD-003), so it's worth it wherever real data varies. Where nothing varies, dynamic generation is pure overhead for zero benefit over a single reusable static file.
**Trade-offs:** Static documents can't be dynamically searched, filtered, or reported on the way generated ones can — acceptable since none of the static-list items are ever transactionally reasoned about (no "find all invoices over ₹X" query will ever be needed against a Training Guide).
**Database impact:** None yet at decision time — see AD-003/AD-004 for the schema this implies once building starts.
**API impact:** None yet — see AD-003/AD-004.
**Future considerations:** Re-apply this same per-client-variance test to any new document type the business proposes, rather than defaulting to "let's generate it."
**Related components:** `src/features/client-workspace/` (`ClientDocument`, `ClientDocumentType`), future PDF template components.

---

## AD-003: PDF generation engine — `@react-pdf/renderer`, not a headless browser

**Date:** 2026-08-03
**Problem:** No PDF generation library exists in the codebase. Need one for AD-002's dynamic documents (invoice first).
**Options considered:**
1. Puppeteer/Playwright — render HTML/CSS via headless Chrome, print to PDF.
2. `@react-pdf/renderer` — pure-JS PDF composition from React components, no browser.
**Chosen solution:** `@react-pdf/renderer`.
**Reason:** The app deploys to Vercel serverless functions. Headless Chrome on serverless requires bundling a Chromium binary (`@sparticuz/chromium` or similar), adds real cold-start latency, and is a recurring source of "works locally, breaks in production" issues on this hosting model. `@react-pdf/renderer` has zero such dependency — it composes the PDF directly, matching this codebase's existing bias toward Vercel-native tooling (Next.js, `@next/third-parties`, Cloudinary for uploads rather than local file handling).
**Trade-offs:** Layout is done via a React-PDF-specific flexbox-like API, not real CSS — pixel-matching a reference design (the founder's invoice template) takes more careful measurement than "just copy the CSS." No support for some advanced CSS the founder's Canva exports might use (gradients, box-shadows) — will degrade gracefully to flat colors/borders where needed.
**Database impact:** None directly — enables AD-004.
**API impact:** New `"use server"` actions to generate + attach each document type, following the existing action patterns (Zod validation, role check, try/catch).
**Future considerations:** If pixel-perfect fidelity to Canva-designed layouts becomes a hard requirement, revisit Puppeteer despite the serverless cost — but only if `@react-pdf/renderer` genuinely can't achieve an acceptable result first.
**Related components:** New `src/lib/pdf/` (or similar) rendering layer, `src/features/sales-crm/` (invoice/receipt), future NDA/Service Agreement templates.

---

## AD-004: Invoice line items are entered manually at generation time, not sourced from `SalesQuote`

**Date:** 2026-08-03
**Problem:** The founder's invoice template shows itemized services with per-item discounts. No existing model (`SalesQuote`, `SalesProject`, `SalesProjectPayment`) stores line-item-level data — all three only track a lump-sum amount.
**Options considered:**
1. Add itemized line items upstream — extend `SalesQuote` (or a new model) to carry structured line items from the moment a quote is sent, and have the invoice read from there.
2. Admin enters/edits line items directly when generating each invoice, independent of any upstream quote structure.
**Chosen solution:** Option 2.
**Reason:** Founder's explicit call. Matches how invoicing actually happens in a real services business — the exact line-item breakdown is often finalized (and can legitimately differ from the original quote wording) at the moment of billing, not fixed at quote time. Avoids a schema change to `SalesQuote` that would ripple into the Proposal/Quote UI for a benefit (auto-populated line items) that's marginal against the cost of re-typing a handful of rows per invoice.
**Trade-offs:** No automatic consistency check between what was quoted and what's invoiced — relies on the admin doing this correctly, same as it would with a manually-issued invoice today. Revisit if invoice/quote mismatches become a real recurring problem.
**Database impact:** New structure needed to hold line items per generated invoice — likely a JSON field on the invoice-generation input/`ClientDocument` metadata rather than a new relational model, since line items are a write-once snapshot at generation time, not something later queried/filtered.
**API impact:** New `generateInvoice` action taking line items as input, producing a PDF, creating a `ClientDocument` (type `INVOICE`) exactly as the existing manual-upload flow does today — no new document-delivery mechanism, only a new document-authoring mechanism.
**Future considerations:** If `SalesQuote` ever grows real itemization for its own reasons (e.g., client-facing line-item quotes), reconsider whether invoice generation should default-populate from it while still allowing edits.
**Related components:** `SalesProjectPayment`, `ClientDocument`, new invoice-generation action + PDF template.

---

## AD-005: Client Success Dashboard is a consolidation of existing data, not a new subsystem

**Date:** 2026-08-03
**Problem:** CEO proposed a "Client Success Dashboard" (project health, payments, milestones, documents, approvals, messages, meetings, warranty, support, completion %) as a new feature.
**Options considered:**
1. Build as a new set of models/queries.
2. Audit what already exists first, build only the gap.
**Chosen solution:** Option 2. Audit found `SalesProject.progressPercent`, `warrantyExpiresAt`, `ProjectMilestone` (ordered timeline), `ProjectUpdate` (progress posts), and `SalesProjectPayment` (paid/pending) already live in the Client Workspace. The real gap is only: an explicit "Project Health" traffic-light status field, and consolidating already-shipped pieces onto one summary view instead of scattering them.
**Reason:** Directly avoids the exact failure mode the CEO flagged — "20 new pages" instead of "one beautiful dashboard." Building new models for data that already exists would itself become the next entry in this file's "why do we have two of these" category.
**Trade-offs:** None identified — this is a strict improvement (less code, same or better outcome).
**Database impact:** One new field likely needed (`SalesProject.healthStatus` enum: ON_TRACK / AT_RISK / COMPLETED, admin-set like `progressPercent` already is).
**API impact:** Consolidation of existing queries into one dashboard-summary query rather than new endpoints.
**Future considerations:** "Support Tickets" from the CEO's list has no existing home — before building a new ticketing subsystem, check whether the existing `SalesLeadMessage`/chat feature can serve this need first.
**Related components:** `src/features/client-workspace/`, `SalesProject`, `ProjectMilestone`, `ProjectUpdate`.

---

## AD-006: Requirement Gathering, QA, and UAT are portal workflows, not documents

**Date:** 2026-08-03
**Problem:** CEO's original brief specified these as PDF-style documents (a form, a report, a checklist).
**Options considered:**
1. PDF/document, filled and uploaded manually.
2. Real in-portal form/workflow with structured data and status tracking.
**Chosen solution:** Option 2 for all three.
**Reason:** Each has an explicit status lifecycle in the CEO's own spec (Pending/Submitted/Approved for the requirement form; bugs found/fixed/pending for QA; client-ticks-and-approves for UAT) — that's a workflow with state, not a static artifact. A PDF can't be queried, can't drive a notification when status changes, and re-introduces exactly the "no WhatsApp, everything through the portal" problem this whole initiative is meant to solve.
**Trade-offs:** More build effort than a document (real form UI, a table, a review flow) versus "upload a filled-in template." Justified by the same reasoning as AD-002 — real per-client structured data belongs in the database, not trapped in a PDF.
**Database impact:** New models needed for each (deferred — not yet scoped in detail; each is its own future architecture decision when actually built).
**API impact:** New CRUD-style actions per workflow, following existing action patterns.
**Future considerations:** When these are actually scoped for implementation, this entry should be superseded by three more specific ADRs (one per workflow) covering their actual schema.
**Related components:** Future `RequirementForm`, `QAReport`/`Bug`, `UATChecklist` models (none exist yet).

---

## AD-007: Architecture freeze until first clients close

**Date:** 2026-08-03
**Problem:** After the platform audit (AD-001 through AD-006), there's a real risk of continuing to plan/build indefinitely instead of converting the P1 list into actual closed deals. Momentum toward "more architecture work" needs an explicit stop condition.
**Options considered:**
1. Keep building P1–P4 roughly in order, no formal constraint.
2. Freeze scope explicitly: only P1 items, client-acquisition work, UX/bug/perf fixes, and SEO/marketing until first clients close.
**Chosen solution:** Option 2.
**Reason:** The entire point of the audit and governance files (ARCHITECTURE_DECISIONS.md, ROADMAP.md, TECH_DEBT.md, SOP.md, PRODUCT_PRINCIPLES.md, BRAND_PRINCIPLES.md) was to stop trading "more features" for "a coherent system." A freeze with a real, sales-focused exit condition (first clients closing) prevents the audit itself from becoming the next thing that quietly expands scope instead of shipping.
**Trade-offs:** P2–P4 items with real value (Requirement Form, Handover Document, Welcome Kit) will sit longer than they otherwise might. Accepted — none of them are blocking a deal from closing today; P1 already covers what is.
**Database impact:** None directly — this is a scope-control decision, not a technical one.
**API impact:** None.
**Future considerations:** Revisit this freeze explicitly once the first client closes, rather than letting it quietly lapse. The condition for lifting it should be named in the same place it was declared (ROADMAP.md's "Current focus window" banner) so it's obvious when it's been met.
**Related components:** ROADMAP.md, PRODUCT_PRINCIPLES.md #12.

---

## AD-008: Document Generation Engine — reusable platform component, not an Invoice Generator

**Date:** 2026-08-03
**Problem:** Phase 1 was originally scoped as "build an invoice generator." Before implementation, the founder redirected this to a reusable engine for every future generated document (Invoice, Receipt, NDA, Service Agreement, Welcome Kit, Completion Certificate, Warranty Certificate), with three specific additions: a document lifecycle, centralized document numbering, and immutable versioning.
**Options considered:**
1. Build an invoice-specific generator now, generalize later when a second document type is needed.
2. Build the shared engine + registry pattern now, since 7 concrete document types are already on the roadmap (not hypothetical).
**Chosen solution:** Option 2.
**Reason:** With 7 known consumers already identified, this isn't premature abstraction - it's building the abstraction at the point real, known need justifies it (PRODUCT_PRINCIPLES.md #6). A template registry (`src/features/documents/lib/registry.ts`) maps a `DocumentType` string key to `{schema, component, numberPrefix, clientDocumentType}` - adding NDA/Service Agreement/etc. in P2 means one new registry entry + one template file, zero changes to the rendering engine, numbering service, or storage layer.
**Trade-offs:** More upfront build than a single-purpose invoice generator - justified by the confirmed P2 document list, not a bet on hypothetical future need. `DOCUMENT_TEMPLATES`'s value type needs one narrow, explained `any` (function-parameter contravariance across a heterogeneous map of concretely-typed entries) - real type safety still enforced twice: once at each entry's own declaration, once at runtime via `entry.schema.parse()` before a template ever sees data.
**Database impact:** Extended `ClientDocument` (not a new parallel model - see reasoning below) with `templateType` (plain string, not a Prisma enum, so new document types never require a migration), `documentNumber`, `lifecycleStatus`, `version`, and a self-relation `supersedesId`/`supersededBy` for the version chain. New `DocumentSequence` model `{type, year, lastNumber}` for atomic, race-safe numbering (same upsert+increment pattern as `claimLead`/commission generation).
**API impact:** `src/features/documents/server/generate.ts`'s `generateDocument(type, data, context)` is the one place data-collection → template-rendering → PDF-generation → storage → delivery gets wired together, document-type-agnostic. `src/features/documents/actions/invoice-actions.ts` is the one type-specific admin-facing action in this pass (line items are admin-authored - AD-004); `src/features/documents/server/receipt-trigger.ts` is called internally from both payment-confirmation paths (`markPaymentPaid`, `verifyProjectPayment`), never admin-triggered directly, since Receipt has no Draft/review step.
**Why extend `ClientDocument` instead of a new model:** A generated invoice and a manually-uploaded welcome kit are still the same underlying concept - a document the client sees in their portal. A parallel `GeneratedDocument` model would recreate the exact "five near-identical tracking tables" problem AD-001's audit flagged. The new fields are simply unused (null/default) for manually-uploaded rows.
**Lifecycle:** `DocumentLifecycleStatus` (DRAFT/GENERATED/SENT/VIEWED/ACCEPTED/REJECTED/ARCHIVED) exists on every document, but Invoice/Receipt in this pass only ever move GENERATED→SENT→(ARCHIVED if superseded) - generation is delivery in the current UX, there's no separate review/send step yet. Accepted/Rejected/Viewed become meaningful once NDA/Service Agreement (P2) need a real "client signs or declines" step.
**Versioning:** Regenerating a document (same salesLead + templateType + relatedPayment) creates a new row (`version + 1`, `supersedesId` pointing at the old row) and flips the old row to ARCHIVED in the same transaction - the old PDF is never deleted or overwritten, only excluded from client-facing queries (`lifecycleStatus: { not: "ARCHIVED" }`, added to `getClientWorkspaceById`/`getClientPayments` in `client-workspace/server/queries.ts`).
**Numbering, one judgment call worth flagging:** Each regeneration gets a **new** document number (INV-2026-00001 → INV-2026-00002 on correction), not the same number at a new version. This matches real invoicing/audit practice (every number should map to exactly one immutable PDF forever, so a bank reconciliation or tax reference is unambiguous) - but it does mean "version 2 of an invoice" and "invoice number 2" are different, easily-conflated concepts. If same-number-across-versions is preferred instead, it's a small change to `generateDocument` (reuse `previous.documentNumber` instead of calling `nextDocumentNumber` when a `previous` row exists).
**Fonts:** `@react-pdf/renderer` can't use `next/font` (no browser, no CSSOM) - the engine embeds real static Geist weight files (Regular/Medium/SemiBold/Bold, SIL OFL licensed, sourced from Vercel's own geist-font repo) rather than falling back to a generic PDF font. Bricolage Grotesque (the site's display face) only ships as a variable font with no static weights, which `@react-pdf/renderer` handles less predictably - Geist alone for documents, which are utilitarian rather than marketing pages, was the right trade rather than risking inconsistent weight rendering.
**Colors:** Brand OKLCH tokens from `globals.css` resolved to hex once (verified against the `culori` library, not hand-trusted) and hardcoded in `lib/theme.ts` - a printed PDF isn't theme-aware, light-mode values only, regardless of the viewer's OS theme.
**Known gap, not a code bug:** Real end-to-end test (generate → upload → download) found Cloudinary returns 401 (`x-cld-error: deny or ACL failure`) on the generated PDF's public URL, despite Cloudinary's own metadata confirming the file is valid (correct A4 dimensions, correct byte size). This is a Cloudinary **account security setting** ("Restricted media types" / PDF+ZIP delivery), not a bug in the upload or generation pipeline - **action item for the founder**: enable PDF/ZIP delivery in the Cloudinary dashboard's security settings before this goes live, or every generated document will 401 for real clients.
**Future considerations:** NDA/Service Agreement (P2) will need a real signature-acceptance step (typed name + timestamp, not DocuSign-grade - already flagged in ROADMAP.md) - that's the first template that will actually exercise SENT→VIEWED→ACCEPTED/REJECTED, worth its own ADR when built.
**Related components:** `src/features/documents/**` (new), `prisma/schema.prisma` (`ClientDocument`, `DocumentSequence`, `DocumentLifecycleStatus`), `src/features/sales-crm/actions/payment-actions.ts`, `src/features/client-workspace/actions/payment-actions.ts`, `src/features/client-workspace/server/queries.ts`.

---

## AD-009: Live notification delivery via polling, not WebSocket/SSE

**Date:** 2026-08-06
**Problem:** Google Ads is going live on `/start-project`; a lead arriving needs to be noticed fast. The existing `Notification` system only ever fetches once, at layout render - someone already sitting on a dashboard page sees nothing new until they manually navigate/refresh. Also surfaced a second, bigger gap while investigating: the `/admin` dashboard (where this actually needs to show up) had no notification bell at all - it was running on an older, separate sidebar shell (`src/components/dashboard/sidebar.tsx`) that predates the richer `DashboardShell`/`Topbar` system every other role's portal already uses.
**Options considered:**
1. WebSocket server (e.g. a custom Node server, or a hosted service like Pusher/Ably) for real push delivery.
2. Server-Sent Events via a Vercel streaming function.
3. Client-side polling against a new `getRecentNotifications` server action.
**Chosen solution:** Option 3 - 15-second polling.
**Reason:** Vercel serverless functions don't hold a persistent connection well (WebSocket needs a long-lived process this hosting model doesn't provide without extra infrastructure; SSE works but ties up a function for the connection's duration and adds real complexity for a need that isn't sub-second). A lead notification needing to be seen "within a few seconds" doesn't justify a new infrastructure dependency (Principle #6, reuse before inventing) - a plain interval + existing server action is the simplest thing that actually satisfies "hear about it without a manual refresh."
**Trade-offs:** Up to 15s latency (acceptable for this use case) instead of instant push. Every open dashboard tab polls independently - fine at current scale, would need a shared/deduped approach (or revisiting Option 1/2) if concurrent admin sessions ever became large enough for the polling traffic itself to matter.
**Database impact:** None - reuses the existing `Notification` model and `getNotificationsForUser` query as-is.
**API impact:** New `getRecentNotifications` server action (`src/features/notifications/actions/notification-actions.ts`) - read-only, `requireUser()`-gated like every other action in that file.
**Future considerations:** If real-time (<1s) delivery is ever genuinely required, revisit SSE first (less infrastructure than a websocket service) before reaching for a third-party push provider.
**Related components:** `src/components/dashboard-shell/notifications/notification-dropdown.tsx`, `src/lib/notification-sound.ts` (synthesized Web Audio chime, no external asset), `src/config/navigation/admin.ts` (populated with the real Founder CRM nav, previously a placeholder stub), `src/app/(dashboard)/layout.tsx` (migrated onto `DashboardShell`, old `src/components/dashboard/sidebar.tsx` removed).

---

## AD-010: Offering catalog seeded from already-public pricing, not invented

**Date:** 2026-08-07
**Problem:** Pre-launch client-dashboard audit (day before Google Ads go live) found `Offering` had **zero rows in the production database**. This wasn't just a client-dashboard issue - `/website-development`'s real pricing section (`getOfferingForDisplay("startup-website-package")`) was silently rendering nothing on a page direct ad traffic would hit, the entire client-portal self-service catalog (`/client/offerings`, dashboard's "Explore offerings"/"Our services", the admin lead-detail service-selection checklist) had nothing to show, and the request-proposal wizard had nothing to request against. Likely cause: every earlier session that built Offering-dependent features tested against temporary rows created and properly cleaned up afterward (per [[feedback_verification_discipline]]) - correct discipline for test data, but nobody separately seeded real, permanent catalog rows for production.
**Options considered:**
1. Build a full admin CRUD UI for managing Offerings, then have the founder populate it manually.
2. Seed the 3 tiers already public and founder-approved on `/pricing` (`src/lib/pricing.ts`, itself commented "exact tiers/pricing/inclusions as given by the client - not altered") as real `Offering` rows.
**Chosen solution:** Option 2 (`prisma/seed-offerings.ts`).
**Reason:** Option 1 is real, legitimate future work (see ROADMAP.md) but is a new module under the current architecture freeze (AD-007) and wasn't blocking tomorrow's launch by itself - the missing *data* was the actual fire. Seeding already-public numbers is safe (not inventing anything - `/pricing`'s own doc comment establishes these are the founder's real, approved figures) and fixes every downstream symptom immediately.
**Trade-offs:** These 3 Offering rows can now only be edited via re-running the seed script or a direct DB edit until admin CRUD (option 1) gets built - acceptable short-term, not a permanent state.
**Database impact:** 3 new `Offering` rows (`startup-website-package`, `business-website-package`, `enterprise-solution`), `status: PUBLISHED`, `audience: BUSINESS`, `visible: true`.
**API impact:** None - existing queries (`getOfferingForDisplay`, `getOfferingsForAudience`) already expected this shape, they just had nothing to return.
**Future considerations:** Build the admin CRUD UI (ROADMAP.md) once catalog changes need to happen more often than "re-run a seed script." Until then, any pricing change on `/pricing` must be manually mirrored into `prisma/seed-offerings.ts` and re-run - the two are not automatically kept in sync.
**Related components:** `prisma/seed-offerings.ts`, `src/lib/pricing.ts`, `src/app/(marketing)/website-development/page.tsx`, `src/app/(portal)/client/offerings/`, `src/app/(portal)/client/dashboard/page.tsx`.

---

## AD-011: Google Ads "Start Project Lead" conversion fires via explicit `gtag` event, not automatic form detection

**Date:** 2026-08-08
**Problem:** The "Start Project Lead" conversion action (`AW-17718751960/4kRyCOyJ3t0cENjl-oBC`) showed "Misconfigured" in Google Ads. Diagnosed live (real form submission, dataLayer inspected mid-flow): the action was configured for Google Ads' automatic "form submission" detection (Event: "Form submission: thank-you"), and the real `gtm.formSubmit` event for this form came through marked `gtm.formCanceled: true` - because `/start-project`'s form uses React's Server Action pattern (`<form action={formAction}>`), which necessarily intercepts the native submit event to route it through JS. Google's automatic detector has no way to distinguish "intercepted and handled by a framework" from "genuinely cancelled," so it never linked a real, successful submission to reaching `/thank-you`, despite the funnel completing correctly every time (confirmed: `gtm.historyChange-v2` fired correctly for the client-side `/start-project` → `/thank-you` `pushState` navigation - GA4/gtag's own SPA tracking is not the problem).
**Options considered:**
1. Keep automatic form-submission detection, try to make the native submit event look "uncancelled" to Google's heuristic.
2. Fire the conversion explicitly from code (`gtag('event', 'conversion', ...)`) on `/thank-you`'s real success path.
**Chosen solution:** Option 2.
**Reason:** Option 1 isn't reliably achievable without abandoning the Server Action form pattern this codebase uses everywhere - not worth destabilizing a working, established form architecture to appease one third-party heuristic. Explicit event firing is deterministic, fully within our control, and is the standard, recommended approach for SPA/React conversion tracking generally (this exact `gtm.formCanceled` failure mode is a known, common friction point with Google's automatic detection on any JS-framework form, not specific to this app).
**Trade-offs:** `/thank-you` has no server-side proof a real submission occurred (it's a plain public route, `noindex` only keeps search engines from listing it - a direct/bookmarked/crawled visit is fully reachable). Mitigated with a same-tab-only sessionStorage handoff (`src/lib/conversion-tracking.ts`) set by the form immediately before `router.push`, consumed (read once, then cleared) by the conversion-firing component - a direct visit finds nothing to consume and fires nothing. Not cryptographically unspoofable, but matches the level of rigor this class of tracking actually needs (deterring accidental/curious direct visits, not a security boundary).
**Database impact:** None.
**API impact:** None - purely client-side. No change to `submitStartProjectLead`, validation, or the redirect itself.
**Future considerations:** If other conversion actions (Purchase, Sign-up) hit the same automatic-detection failure mode - likely, since they'd observe the same Server-Action-intercepted form pattern - apply this identical fix: explicit `gtag('event', 'conversion', ...)` on that flow's real success path, gated the same sessionStorage-handoff way. `conversion-tracking.ts`'s key constant is scoped to this one flow (`START_PROJECT_CONVERSION_KEY`) - a second flow needs its own key, not a shared one, so two conversions in flight in the same tab can't cross-fire each other's event.
**Related components:** `src/lib/conversion-tracking.ts` (new), `src/components/analytics/start-project-conversion.tsx` (new), `src/components/forms/start-project-form.tsx`, `src/app/(marketing)/thank-you/page.tsx`.

---

## AD-012: `LeadMeeting` as a separate model from `SalesLeadMeeting`, and reminder delivery via external cron ping

**Date:** 2026-08-08
**Problem:** The founder wanted to schedule a follow-up meeting directly from a raw `Lead`'s admin detail page (the first-touch call, before any `SalesLead`/client account exists), with a reminder notification 30 minutes before it starts. A meeting-scheduling feature already existed, but only on `SalesLead` (post-claim), and no notification anywhere in this codebase had ever fired on a delay before - every existing notification fires immediately at action time.
**Options considered:**
1. Make `SalesLeadMeeting.salesLeadId` nullable and add an alternate `leadId` FK, so one table serves both parent types.
2. A new, separate `LeadMeeting` model with the same shape, scoped to `Lead` only.
3. For the reminder: register a Vercel Cron entry in `vercel.json` running every few minutes.
4. For the reminder: build the check as a plain authenticated API route, pinged externally on a schedule (not registered as a Vercel Cron Job at all).
**Chosen solution:** Option 2 for the model, Option 4 for the reminder trigger.
**Reason:** Every existing `SalesLeadMeeting` query/component assumes `salesLeadId` is always present (non-nullable); retrofitting it to serve two different parent types would touch every one of those call sites for a relationship that's conceptually different anyway (pre-conversion vs. post-claim). A second small model mirroring the same field shape (reusing the same `MeetingStatus`/`PreferredContactMethod` enums) was cheaper and safer than that migration. For the reminder trigger: Vercel's Hobby/free tier caps native Cron Jobs at once per day regardless of what's declared in `vercel.json` - not frequent enough for a 30-minutes-before reminder. A plain API route, authenticated with the same `CRON_SECRET`/`Authorization: Bearer` scheme the existing lead-intelligence crons already use, and pinged by a free external service (e.g. cron-job.org) every few minutes, achieves the same result with zero new secrets and zero hosting cost.
**Trade-offs:** The reminder depends on an external, third-party pinging service staying configured and running - if it's ever removed/misconfigured, reminders silently stop (mitigated by the endpoint being simple and idempotent, so re-adding the ping catches back up immediately). Two near-identical meeting models now exist (`LeadMeeting`, `SalesLeadMeeting`) rather than one - accepted given the alternative's blast radius across existing SalesLead code.
**Database impact:** New `LeadMeeting` model (`prisma/migrations/20260808085427_lead_meeting_and_reminders`). `reminderSentAt DateTime?` added to both `LeadMeeting` and `SalesLeadMeeting` as the not-reminded-twice guard. New `LeadEventType.MEETING_SCHEDULED` and `NotificationType.MEETING_REMINDER` enum values.
**API impact:** New `scheduleLeadMeeting`/`updateLeadMeetingStatus` actions (`src/features/leads/actions/meeting-actions.ts`), new `GET /api/cron/meeting-reminders` (checks both meeting tables, not registered in `vercel.json`).
**Future considerations:** If a paid Vercel plan is adopted later, this could move to native Vercel Cron (add a `vercel.json` entry, drop the external pinger) with no other code changes - the endpoint's own auth already matches Vercel's convention. If a third meeting-scheduling context appears, revisit whether a shared/polymorphic model is worth the migration at that point.
**Related components:** `prisma/schema.prisma` (`LeadMeeting`, `SalesLeadMeeting.reminderSentAt`), `src/features/leads/actions/meeting-actions.ts`, `src/features/leads/validation/meeting-schemas.ts`, `src/components/dashboard/lead-meeting-panel.tsx`, `src/features/notifications/server/meeting-reminder.ts`, `src/app/api/cron/meeting-reminders/route.ts`.

---

## AD-013: `B2B_ONLY_MODE` — one flag to pause student/training promotion, not deletion

**Date:** 2026-08-08
**Problem:** The founder is pausing all training/instructor activity for 2-3 months to focus exclusively on closing B2B clients, and wants the public site to present as business-only for that period - no student sign-up path, no Training nav link, no training mention on the homepage, training programs excluded from search results.
**Options considered:**
1. Delete/comment out the student registration UI, nav links, and training pages outright.
2. A single exported boolean (`B2B_ONLY_MODE` in `src/config/site.ts`) that every affected surface reads, with everything conditionally hidden rather than removed.
**Chosen solution:** Option 2.
**Reason:** This is an explicitly temporary, reversible business decision (2-3 months, not permanent) - commenting out or deleting real, working code for a temporary pause creates exactly the kind of bit-rot/merge-conflict risk this codebase's own conventions already avoid elsewhere. A single flag is grep-able (every touch point references the same constant), reverts in one line, and - critically - was verified safe before shipping: zero real `STUDENT`-role users and zero `OfferingEnrollment` rows exist in production today, so nothing active gets stranded either way.
**Trade-offs:** Anyone who still finds `/training` or `/training/[slug]` via a direct link or old bookmark can still reach them (deliberately not 404'd - the content is real and accurate, just not promoted); this is intentional, not an oversight. The homepage's `EcosystemNote` section (a quiet mention of the training pipeline as a quality signal for business visitors) is also hidden while the flag is on, even though its content stays true - full B2B consistency was judged more valuable than keeping that one section during the pause.
**Database impact:** None.
**API impact:** None - this only gates UI rendering and page `robots` metadata, not RBAC or the register/login server actions themselves. Existing `STUDENT`-role accounts (none currently exist) would continue to log in and reach `/student/*` exactly as before if any existed.
**Future considerations:** Flip `B2B_ONLY_MODE` to `false` in `src/config/site.ts` to fully restore student-facing promotion - every other file listed below reads that same constant, nothing else needs to change.
**Related components:** `src/config/site.ts` (the flag + corrected sitewide title/description, which had drifted student-first even after the homepage itself was already rebuilt business-first), `src/components/shared/navbar.tsx`, `src/components/shared/footer.tsx`, `src/components/forms/register-form.tsx`, `src/app/(auth)/register/page.tsx`, `src/app/(marketing)/page.tsx`, `src/app/(marketing)/training/page.tsx`, `src/app/(marketing)/training/[slug]/page.tsx`.

---

## AD-014: Client message notifications must always reach an admin, not just an assigned rep

**Date:** 2026-08-09
**Problem:** `sendMessageToStaff` only notified `lead.assignedTo?.userId` - real production data showed every current `SalesLead` sits unassigned, so client messages were notifying nobody at all. The founder reported "I am not able to receive chat of a particular customer" - a real, currently-happening data loss, not a hypothetical. Investigation also found a genuine, already-sitting unread message from a real client that had never surfaced anywhere.
**Options considered:**
1. Keep notifying only the assigned rep; fix by requiring assignment before a lead can receive messages.
2. Always notify every `ADMIN`/`SUPER_ADMIN` (plus the assigned rep if one exists), and add a real "unread" concept with an admin-facing inbox as a backstop for missed/dismissed notifications.
**Chosen solution:** Option 2.
**Reason:** Requiring assignment first would block a client's message from ever reaching anyone until a human remembers to assign the lead - worse, not better. This is effectively a solo-founder operation right now; the founder should see every client message regardless of who (if anyone) is assigned, same as `notifyLeadClaimed` already notifies all admins for lead-claim events. A single notification is also inherently missable (dismissed, phone off, etc.) - a persistent, always-current "Messages" widget on the dashboard is the real fix for "receive chat," not just a better-targeted push.
**Trade-offs:** Every admin now gets notified on every client message across every lead, not just their own - acceptable at current team size; if the team grows meaningfully, this may need scoping back down (e.g., only admins + assigned rep, once assignment is actually being used consistently).
**Database impact:** `SalesLeadMessage.readAt DateTime?` added (`prisma/migrations/20260809184711_sales_lead_message_read_at`). Null until a staff member views the thread; a message never counts as unread to its own sender.
**API impact:** `getMessagesForLead` now marks a lead's client-sent messages read as a side effect of staff fetching the thread (viewing = read, no separate click). New `getUnreadMessageThreads(viewer)` query, RBAC-scoped the same way as every other sales-crm query.
**Future considerations:** If a team grows past the founder, revisit whether all-admins-always-notified is still right, or whether it should narrow to assigned-rep-plus-admin-fallback-only-if-unassigned.
**Related components:** `prisma/schema.prisma` (`SalesLeadMessage.readAt`), `src/features/client-workspace/actions/message-actions.ts` (`sendMessageToStaff`), `src/features/sales-crm/server/queries.ts` (`getMessagesForLead`, `getUnreadMessageThreads`), `src/features/sales-crm/components/admin/unread-messages-widget.tsx` (new), `src/app/(dashboard)/admin/sales-crm/dashboard/page.tsx`.

---

## AD-015: Qualified Client system — extend Sales CRM, don't build a parallel one

**Date:** 2026-08-11
**Problem:** Founder asked for a "qualified clients" tab, separate from the mixed Leads/Businesses inbox, where a lead becomes a manageable client the moment its status is set to Qualified — with discovery forms, per-client messaging, project status, and no duplicate records. A full 21-section brief specified admin pages, client pages, DB design, and security requirements as if building a new module from scratch.
**Options considered:**
1. Build a new parallel `Client`/`QualifiedClient` model + admin section + client dashboard, as literally specified in the brief.
2. Audit first (per the standing CTO workflow) - most of what was asked already exists as `SalesLead`/`SalesProject`/Client Workspace (AD-001, "AI Sales Workspace" released item); build only the real gaps.
**Chosen solution:** Option 2. Audit found: `SalesLead` already has status, assignment, quotes, meetings, messages (with unread tracking, AD-014), notes, documents, and a full activity timeline; `SalesProject` already tracks delivery status/progress/payments/milestones; the Client Workspace portal already scopes a client to only their own data. The two real gaps: (a) no bridge from a raw inbound `Lead` (contact form/`/start-project`) into `SalesLead` - only `Business` and `OfferingRequest` had that bridge; (b) no client-fillable Discovery & Requirements Form - the existing `SalesLeadDiscovery` is an admin-only internal checklist, not something a client fills via a link.
**Reason:** Building a second "clients" table/page duplicating `SalesLead`'s columns would have recreated the exact failure mode AD-001 already fought off - two near-identical tracking systems for the same real-world thing. Everything genuinely new here (the qualification bridge, the discovery form) plugs directly into the existing Sales CRM instead.
**Trade-offs:** The founder's "Clients" tab is a filtered view of the existing Sales CRM Leads list (`?status=WON`) rather than a visually distinct new page - less novel-feeling, but zero duplicate data and zero new nav items to keep in sync. `SalesLeadStatus` has no literal `QUALIFIED` value - `Lead.status = CONVERTED` (already existed, unused) is the real trigger, relabeled "Qualified" in the BUSINESS-leadType UI only.
**Database impact:**
- `Lead`: added `promotedSalesLeadId` (unique FK to `SalesLead`), `promotedAt`, `promotedById` - same bridge shape as `Business.promotedSalesLeadId`/`OfferingRequest.promotedSalesLeadId`.
- `SalesProject`: `salesLeadId` changed from `@unique` to a plain indexed FK (1:many) - a client can now have multiple projects (a repeat/expanded engagement is a new Project on the same SalesLead, not a second SalesLead). Added `name` (required - a project needs a label once more than one can exist), `type` (free text), `priority` (reuses `LeadPriority`).
- New `DiscoveryForm` model + `DiscoveryFormStatus` enum (Draft/Sent/Opened/In Progress/Submitted/Reviewed) - client-fillable, token-linked (same unguessable-token pattern as `Proposal.token`), fields mirror the real 4-section intake document verbatim.
**API impact:** `qualifyBusinessLeadTx` runs inside `updateLead`'s existing transaction (`src/actions/crm.ts`) - a BUSINESS lead's status change to CONVERTED and its SalesLead link/create either both commit or neither does. New `src/features/discovery-forms/` feature (actions, queries, token resolution, email). `convertLeadToProject` no longer blocks a second call on the same lead - it always creates one more `SalesProject`.
**Multi-project UI impact:** Client Workspace's Payments/Progress/Timeline moved off `ClientWorkspaceDetail`'s shared tabs onto a new dedicated per-project page (`/client/projects/[id]/[projectId]`) - flattening several projects' installments/milestones into one set of tabs would have been ambiguous. Admin's `SalesLeadDetail` header now lists every project as its own link instead of one "View project" button.
**Judgment call, not silently made:** the brief's "if the same email already belongs to a client, detect the existing client and offer to link the lead rather than creating a duplicate" is implemented as an automatic link (not a confirmation dialog) when email/phone matches an existing `SalesLead` - safe given an exact match is unambiguous, but simplified from "offer" to "just do it." Logged in TECH_DEBT.md if a confirm-first flow is ever wanted.
**Future considerations:** `DiscoveryForm.salesProjectId` doesn't yet have a declared Prisma relation (see TECH_DEBT.md) - a plain FK for now, to avoid a second migration in the same pass. If a fourth "someone wants to talk to us" bridge is ever proposed, re-run AD-001's test before adding a new one.
**Related components:** `prisma/schema.prisma` (`Lead`, `SalesLead`, `SalesProject`, `DiscoveryForm`), `src/actions/crm.ts`, `src/features/leads/server/qualify.ts`, `src/features/discovery-forms/**` (new), `src/features/client-workspace/components/client/client-project-detail.tsx` (new), `src/features/sales-crm/components/admin/sales-lead-detail.tsx`, `src/features/sales-crm/actions/project-actions.ts`.

---

## AD-016: Four pre-launch fixes to the Qualified Client system, from a founder-reviewed audit

**Date:** 2026-08-11
**Problem:** A full walkthrough of AD-015's Qualified Client system (requested before onboarding the first real client) surfaced four real gaps: (1) lead-qualification notifications only reached an assigned salesperson, and leads sit unassigned by default - qualification could happen with nobody told; (2) the `SalesLead` dedupe check on qualification was a check-then-act race under Postgres's default isolation - two leads sharing a phone/email, qualified close together, could each create a separate `SalesLead`; (3) `DiscoveryFormStatus` had no `EXPIRED` value - expiry was computed at read time only, never persisted, so the admin panel never showed a form as expired and "Resend" re-mailed the same dead token; (4) the "Clients only" quick filter on the Sales CRM Leads list gated on `status=WON`, while every `SalesLead` - WON or not - already has full Client Workspace access, so a qualified prospect still in negotiation didn't show up under it.
**Options considered (per issue):**
1. Notification: keep assignee-only, or always notify all admins (same as AD-014's message fix) plus the assignee if one exists.
2. Dedupe race: a DB-level unique constraint on `SalesLead.phone`/`email`, or transaction-level concurrency safety (Serializable isolation + bounded retry on write conflict), or both.
3. Expiry: leave expiry ephemeral/read-only, or add a persisted `EXPIRED` status and make resend regenerate the link, or make resend just extend the existing token's `expiresAt`.
4. Clients filter: rename only, or restructure the whole view to show every `SalesLead` by default with an optional pipeline-stage quick-filter.
**Chosen solution:**
1. `notifyAllAdmins()` (new shared helper, `src/features/notifications/server/creation.ts`) - fans out to every ADMIN/SUPER_ADMIN plus an optional extra recipient (the assignee, if any), deduped. Also used to refactor `notifyDiscoveryFormSubmitted`, which already followed this exact pattern under AD-014, into one shared implementation instead of two copies.
2. Serializable isolation on `updateLead`'s transaction, but only when a qualification is actually happening (`willQualify`) - not on every plain status/priority edit, which doesn't need it. Wrapped in a bounded retry loop (3 attempts) that catches Prisma's `P2034` (write conflict) and re-runs; a genuine conflict resolves on the first retry since the dedupe read then sees the other transaction's now-committed row. **Not** paired with a DB-level unique constraint on `SalesLead.phone`/`email` - production data already has two pre-existing duplicate placeholder rows (from earlier test sessions) that would fail such a migration; the transaction-level fix is correct regardless of existing data cleanliness and doesn't require touching those rows. Revisit adding the constraint once that test data is confirmed safe to clean up.
3. Added `DiscoveryFormStatus.EXPIRED`, persisted by `resolveDiscoveryFormToken` the first time it detects a lapsed `expiresAt` (same "write on read" precedent as `Proposal`'s own `EXPIRED` handling). `resendDiscoveryForm` now always issues a fresh `token` + `expiresAt` on the same row (never a second form) and resets `status`/`sentAt`/`openedAt` - the old token is overwritten, not duplicated, so it dies immediately. The authenticated client-portal path (which never used the token) is unaffected by expiry - only the public link is time-bound.
4. `/admin/sales-crm/leads` reframed as "Qualified Clients," showing every `SalesLead` by default. Added a `statusIn` filter (`SalesLeadFilters`) alongside the existing single `status`, and a `PIPELINE_STAGES` grouping (`src/features/sales-crm/lib/pipeline-stages.ts`): Qualified (NEW/CONTACTED/FOLLOW_UP/INTERESTED/MEETING_SCHEDULED) / Proposal Sent / Negotiating / Won / Lost, rendered as quick-filter chips. `stage` (coarse) and `status` (exact, from the existing filters toolbar) are mutually exclusive - picking one clears the other via a new `setFilters()` atomic multi-key update on `useSalesLeadFilters`, replacing the old single-key `setFilter` racing-push risk.
**Reason:** All four were confirmed as real, not hypothetical - see the founder's own review decision recorded in this session. Fixes 1 and 4 were changes to what already existed (a gap in an already-shipped notification path; a mislabeled filter), not new subsystems. Fix 2 deliberately avoided a data migration with unknown blast radius in favor of a mechanism that's correct on its own terms. Fix 3 follows an already-established pattern in this codebase (`Proposal.status` EXPIRED) that DiscoveryForm should have matched from the start.
**Trade-offs:** Serializable transactions have a real, if rare, abort rate under genuine contention - acceptable here since it only activates on the qualify path, and the retry loop absorbs it transparently in the overwhelming majority of cases. The "Qualified Clients" stage buckets are opinionated (`ON_HOLD` isn't folded into any of the five quick chips) - still reachable via the full status dropdown, just not a quick-filter chip.
**Database impact:** `DiscoveryFormStatus` enum gained `EXPIRED` (`prisma/migrations/20260811050824_discovery_form_expired_status`). No other schema changes - fixes 1, 2, and 4 are code-only.
**API impact:** `notifyAllAdmins` (new, `src/features/notifications/server/creation.ts`), `updateLead`'s qualify path now retries under `P2034`, `resendDiscoveryForm` rewritten to regenerate rather than resend, `getSalesLeads`/`buildSalesLeadWhere` accept `statusIn`, `useSalesLeadFilters` gained `setFilters`.
**Future considerations:** Once the two pre-existing test-data duplicate phone/email rows in `SalesLead` are confirmed safe to remove, add a real `@unique` constraint on `phone` as defense-in-depth alongside the transaction-level fix (belt and suspenders, not a replacement for it).
**Related components:** `src/actions/crm.ts`, `src/features/leads/server/qualify.ts`, `src/features/notifications/server/creation.ts`, `src/features/discovery-forms/server/token-queries.ts`, `src/features/discovery-forms/actions/discovery-form-actions.ts`, `src/features/sales-crm/lib/pipeline-stages.ts` (new), `src/features/sales-crm/server/queries.ts`, `src/features/sales-crm/hooks/use-sales-lead-filters.ts`, `src/app/(dashboard)/admin/sales-crm/leads/page.tsx`, `prisma/schema.prisma`.

---

## AD-017: Token-based client invite links, replacing the password-reset-email hack, plus two soft identity carry-overs

**Date:** 2026-08-11
**Problem:** The founder identified a real gap: a prospect who submits `/start-project` under one email and later self-registers a portal account under a different one ends up with two disconnected identities - the real one (linked to their `SalesLead`, with actual quotes/messages) and a second, permanently empty one. Root cause traced in code: `/register` never checks against `Lead`/`SalesLead` by email, and `inviteClientToPortal` only matched by exact email, silently creating a second `User` if the lead's email had no account yet even when the same person already had one under a different address. Separately, client-account provisioning itself was riding on the password-reset-email flow (not a real "create your account" experience) - fragile if that specific email never lands, and not really what it claims to be.
**Options considered:**
1. Hard-lock `/register`'s email (and force a specific Google account) once a visitor is identified as a recent lead.
2. Soft carry-over: prefill and explain, never lock - visitor can always override.
3. Keep the password-reset-email account-provisioning hack, or replace it with a real invite-token flow (same pattern as `Proposal`/`DiscoveryForm`).
**Chosen solution:** Option 2 for identity carry-over (founder's explicit call - a hard lock would wrongly block a second real person on a shared/office device, and Google's `login_hint` can't be enforced as a true restriction anyway, only suggested). Option 2 (rebuild) for provisioning: a real `/invite/[token]` flow.
- **`SalesLead`** gained `inviteToken`/`inviteTokenExpiresAt`/`inviteSentAt`/`inviteOpenedAt`/`inviteAcceptedAt` - admin generates a link (`sendClientInviteLink`, replacing `inviteClientToPortal`'s immediate-User-creation path) or it's auto-attached to a quote email when the lead has no portal account yet (`createAndSendQuote`, per the founder's own "when we send a quote, offer the account then" idea). The person visits `/invite/[token]`, sets their own name + password, and is signed straight in via `signIn("credentials", ...)` and redirected to `/client/dashboard` - the same `signIn()`-throws-a-redirect pattern `loginUser` already uses, just reused here instead of a password-reset detour. If an account already exists at that exact email, the admin action still links immediately (no token needed - they can already sign in).
- **`/start-project`**: if a CLIENT is already logged in, prefill (never lock) the email field from their session - a server-side `auth()` read passed down as a prop, since this app doesn't run a client-side `SessionProvider`.
- **`/register`**: `submitLead` (the one function behind both Contact and start-project) now drops a 30-day httpOnly cookie (`RECENT_LEAD_EMAIL_COOKIE`) with the submitted email. `/register` reads it server-side and prefills the email field with an explanatory note, and passes it as a Google `login_hint` too - both cases stay fully editable.
**Reason:** All three pieces attack the same problem (don't let a real client's identity split in two) at different points in the funnel, per the founder's own three-stage breakdown (account-first, form-first, and the admin-invite backstop). None of them required inventing new mechanics - the token-link pattern was already proven twice (`Proposal`, `DiscoveryForm`), and the cookie-prefill pattern already existed for `PENDING_ROLE_COOKIE`.
**Trade-offs:** The invite-acceptance page collects email/password only, no Google sign-in option there (the target email is already fixed by the token, so "which Google account" adds a UI branch for a case the plain form already handles cleanly) - a fast follow if ever wanted. `googleSignIn`'s new `login_hint` behavior can't be verified end-to-end without a real Google OAuth round-trip; verified instead that the parameter is correctly threaded through to `signIn()`.
**Database impact:** `SalesLead` gained the five invite-tracking fields above (`prisma/migrations/20260811075326_sales_lead_invite_token`).
**API impact:** `sendClientInviteLink` + `acceptClientInvite` (replacing `inviteClientToPortal`) in `client-invite-actions.ts`; new `ensureClientInviteToken`/`sendClientInviteEmail` in `invite-creation.ts` (shared by the admin action and `createAndSendQuote`); new `resolveClientInviteToken`/`recordClientInviteOpen` in `invite-queries.ts`; `submitLead` (`src/actions/leads.ts`) now sets a cookie as a side effect; `googleSignIn` accepts an optional `login_hint`. `isNextRedirectError` moved out of `src/actions/auth.ts` into a plain `src/lib/next-redirect.ts` module - a `"use server"` file can only export async Server Actions, and a sync helper needed by more than one action file can't live there anymore now that a second file (`client-invite-actions.ts`) needs it too.
**Future considerations:** If the "hard lock" question ever needs revisiting (e.g. real duplicate-identity incidents start happening despite the soft carry-over), the fields/plumbing here already support tightening it - the decision not to lock was a judgment call about current risk, not a technical ceiling.
**Related components:** `prisma/schema.prisma` (`SalesLead`), `src/features/sales-crm/lib/invite-token.ts`, `src/features/sales-crm/server/invite-creation.ts`, `src/features/sales-crm/server/invite-queries.ts`, `src/features/sales-crm/actions/client-invite-actions.ts`, `src/features/sales-crm/actions/quote-actions.ts`, `src/features/sales-crm/components/public/accept-invite-form.tsx` (new), `src/app/invite/[token]/page.tsx` (new), `src/actions/leads.ts`, `src/actions/auth.ts`, `src/lib/next-redirect.ts` (new), `src/lib/auth-constants.ts`, `src/components/forms/register-form.tsx`, `src/components/forms/google-signin-button.tsx`, `src/components/forms/start-project-form.tsx`, `src/app/(auth)/register/page.tsx`, `src/app/(marketing)/start-project/page.tsx`.

---

## AD-018: Client Onboarding Form — a real Claude Design handoff, wired to the same token architecture as DiscoveryForm

**Date:** 2026-08-11
**Problem:** The founder designed a 7-section post-approval project kickoff form (company/billing info, project scope confirmation, brand asset uploads, technical access, content uploads, communication preferences, start-date approval) in Claude Design and wanted it live: admin sends it to a client with one click from the project's own page, the client fills it in (including real file uploads), and the response lands back in the CRM for review.
**Options considered:**
1. Recreate the design's own standalone markup/CSS pixel-for-pixel as a one-off page, matching the handoff bundle's own README instruction literally.
2. Transcribe the design's real content (labels, placeholders, required marks, section order, warning/footer copy) faithfully, but build it with this codebase's existing form primitives (`Input`/`Textarea`/`Checkbox`/`Section`/`Field`) and the same token/status/notify architecture already proven twice (`Proposal`, `DiscoveryForm` - AD-015).
**Chosen solution:** Option 2, matching the precedent already set for `DiscoveryForm` in this same codebase - visual consistency with the rest of the admin/client portal matters more than a bespoke one-off palette on a single form, and the architecture (token resolution, expiry, status lifecycle, admin send/resend/review panel) doesn't need reinventing a third time.
**Reason:** `OnboardingForm` is scoped to `SalesProject` (a required FK), not `SalesLead` - unlike `DiscoveryForm`, this form asks "Project name" and "Approved scope," which only make sense once a specific engagement exists, and a client with multiple projects (AD-015's multi-project support) needs a separate onboarding form per project. File uploads (7 fields across two sections, some single-file, some multi-file) are one relational `OnboardingFormUpload` model discriminated by a `field` enum, mirroring `SalesLeadAttachment`'s existing shape rather than inventing per-field URL/array columns. The fillable-form component uses native FormData + uncontrolled inputs (not the plain-object pattern `DiscoveryFormFields` uses) specifically because it needs to carry real `File` objects alongside text in one submission - matching this codebase's own `loginUser`/`registerUser` FormData convention rather than a one-off shape.
**Trade-offs:** Not a pixel-perfect recreation of the design's own inline styles (Archivo font, `#ec3013`/`#f3f2f2` palette) - the app's existing design tokens are used instead, so the form reads as part of the same product as everything else in the CRM/portal rather than a visually distinct one-off. All content (labels, placeholders, required marks, the technical-access security warning, the confirmation checkbox copy, the thank-you message, the footer disclaimer) was transcribed verbatim from the `.dc.html` handoff, so nothing was invented.
**Database impact:** New `OnboardingForm` model (~30 content fields across 7 sections + lifecycle fields) and `OnboardingFormUpload` model, plus `OnboardingFormStatus` (Draft/Sent/Opened/Submitted/Reviewed/Expired - same lazily-persisted `EXPIRED` pattern as AD-016's `DiscoveryFormStatus` fix, learned up front this time) and `OnboardingUploadField` enums (`prisma/migrations/20260811091857_onboarding_form`). `SalesProject`, `TeamMember` gained the corresponding relations.
**API impact:** New `src/features/onboarding-forms/**` feature (token/expiry lib, status labels, token resolution + first-open tracking, project-scoped queries, send/submit email + admin-notify, `uploadOnboardingFormFiles` via the existing `uploadFile()` Cloudinary helper extended with an `"onboarding-forms"` folder, Zod content schema, and `sendOnboardingForm`/`resendOnboardingForm`/`markOnboardingFormReviewed`/`submitOnboardingFormByToken`/`submitOnboardingFormAuthenticated` actions). `getSalesProjectById` now includes `onboardingForms`. New public route `/onboarding/[token]`, new response-detail routes under both `/admin/sales-crm/projects/[id]/onboarding-forms/[formId]` and `/sales/projects/[id]/onboarding-forms/[formId]`, new "Onboarding" tab on the client-portal per-project page (`/client/projects/[id]/[projectId]`).
**Future considerations:** Uploaded files currently link out to their raw Cloudinary URL from the admin response view with no lifecycle tracking (unlike `ClientDocument`, which has archival/versioning) - fine for a first pass since these are one-time kickoff assets, not evolving deliverables; revisit if onboarding uploads ever need the same document lifecycle.
**Related components:** `prisma/schema.prisma` (`OnboardingForm`, `OnboardingFormUpload`, `SalesProject`, `TeamMember`), `src/lib/cloudinary.ts`, `src/features/onboarding-forms/**` (new), `src/features/sales-crm/server/queries.ts`, `src/features/sales-crm/components/admin/sales-project-detail.tsx`, `src/features/client-workspace/components/client/client-project-detail.tsx`, `src/app/onboarding/[token]/page.tsx` (new), `src/app/(dashboard)/admin/sales-crm/projects/[id]/onboarding-forms/[formId]/page.tsx` (new), `src/app/(portal)/sales/projects/[id]/onboarding-forms/[formId]/page.tsx` (new), `src/app/(portal)/client/projects/[id]/[projectId]/page.tsx`.

---

## AD-019: Student certificate generation — a freeze exception, on-demand PDF rendering instead of object storage

**Date:** 2026-09-12
**Problem:** The founder provided a real certificate design (Certificate_Template.pptx/.pdf/.png, one design) and asked for a full generate-and-verify pipeline for the GenAI course: personalized PDF, unique certificate ID, QR-verified public page, admin management. This is unambiguously a new major module under AD-007's active architecture freeze ("no new major modules unless they directly help acquire/serve/retain a client"). Separately, the existing `Certificate` model was a prepared-but-unused extension point (`TECH_DEBT.md`), and the existing Document Generation Engine's PDF storage (Cloudinary) is already known-broken for PDF delivery in production (AD-008's "Known gap").
**Options considered:**
1. Defer entirely until the freeze lifts (first client closes).
2. Build it now as a freeze exception, reasoning that "Certificate of completion" is already sold copy (course detail page, training page, and the post-purchase welcome email all promise one today, with zero fulfillment behind it) - this is completing an already-sold feature, not speculative new scope.
3. For PDF storage: reuse Cloudinary via the Document Generation Engine's existing `uploadFile` path, accepting the same 401/restricted-media-types gap AD-008 already has open.
4. For PDF storage: provision Vercel Blob (recommended in this session's earlier Functions-Storage audit) before building this.
5. For PDF storage: never persist the PDF at all - store only the data (recipient name, course name, dates, certificate number) on the `Certificate` row, and re-render the PDF from that snapshot on every view/download request.
**Chosen solution:** Option 2 for scope, confirmed with the founder before writing code (this session's own AskUserQuestion exchange). Option 5 for storage.
**Reason:** Option 2 - the founder confirmed proceeding and logging the exception, per AD-007's own instruction to name the reasoning where the freeze was declared rather than let it quietly lapse. Option 5 - Option 3 would just relocate AD-008's known-broken Cloudinary PDF gap onto a second document type; Option 4 requires provisioning new infrastructure (enabling Vercel Blob in the dashboard) that doesn't exist yet, for a feature this session confirmed doesn't structurally need it. A single-page A4-landscape certificate renders in well under a second via the same `@react-pdf/renderer` engine already proven for invoices/receipts, and every field it needs is already snapshotted on the `Certificate` row - there is nothing to gain from persisting the bytes and a real, currently-open storage bug to inherit by doing so. "The smallest robust solution" here is literally having no storage layer.
**Trade-offs:** Every view/download re-renders the PDF (a few hundred ms of CPU) rather than serving a cached file - acceptable at current and expected near-term volume; revisit only if certificate downloads become a genuine hot path. The certificate-register description paragraph is *not* snapshotted onto the row (only recipientName/courseName/completionDate are) - it's read live from `COURSE_CERTIFICATE_CONFIG` at render time, so editing that config's wording would cosmetically change how *already-issued* certificates render on a future re-download. Accepted deliberately: this text is decorative/descriptive, not identity-bearing, and avoiding a migration for it was judged worth that small inconsistency risk.
**Database impact:** Extended the existing unused `Certificate` model (`prisma/migrations/20260912060000_add_certificate_generation_fields`) rather than creating a new one - added `certificateNumber` (unique, public-facing), `templateId` (plain string, not an enum, so a 2nd/3rd visual design later is a new template file + registry entry, zero migration), `recipientName`/`recipientEmail`/`courseName`/`completionDate` (snapshotted at issuance, deliberately not read live off `User`/`Offering`), `revokedAt`/`revokedReason`, `updatedAt`. Added `REVOKED` to `CertificateStatus`. Reused `DocumentSequence` (extending `nextDocumentNumber` with an optional `digits` param, default 5, certificates use 6) for atomic, race-safe numbering (`STV-GAI-2026-000001`) instead of a parallel sequence mechanism.
**API impact:** New `src/features/certificates/**`: `server/eligibility.ts` reuses `getAccessPolicyForEnrollment`'s `canViewCertificates` (enrollment.status === COMPLETED) as the one eligibility gate, rather than inventing a second completion check; `server/issuance.ts`'s `getOrIssueCertificate` is the only place a row is ever created, idempotent via `enrollmentId`'s unique constraint (catches P2002 on a lost race, mirroring `fulfillGuestOrder`'s claim pattern); `server/render.ts` renders on demand (see above); `server/email.ts` reuses the guest-checkout email HTML skeleton. New routes: `/api/certificates/[certificateNumber]/pdf` (session-authenticated, owner-or-admin, re-renders fresh every call, `?mode=download` toggles Content-Disposition), `/verify/[certificateId]` (public, read-only, exposes only recipientName/courseName/dates/status - never email or internal id), `/admin/certificates` (search by name/email/certificate ID, revoke action). Wired a `CertificateCard` into the existing `/student/learning/[enrollmentId]` page in place of the generic "Certificates - Coming soon" placeholder once `canViewCertificates` is true, and a certificate-preview section into `course-detail-view.tsx`, gated on `getCourseCertificateConfig(offering.slug)` returning non-null so no other course shows a preview it can't back up.
**Future considerations:** Only one course (`generative-ai-prompt-engineering`) is configured in `src/features/certificates/lib/course-config.ts`; adding a second course's certificates is one new entry there (prefix + course name + description), no schema or rendering changes. Only one visual template (`template-01.tsx`) exists - the founder confirmed the other 2 "formats" mentioned were the same single design in different file formats (png/pptx/pdf), not 3 designs; the `templateId` field and a template registry are already shaped for a real second design whenever one arrives. If certificate volume or download frequency ever justifies it, Option 4 (Vercel Blob) is the natural upgrade path for caching rendered PDFs - not required today.
**Related components:** `prisma/schema.prisma` (`Certificate`, `CertificateStatus`), `src/features/documents/server/numbering.ts` (extended), `src/features/documents/components/qr-code.tsx` and `signature-block.tsx` (reused as-is), `src/features/certificates/**` (new), `src/app/api/certificates/[certificateNumber]/pdf/route.ts` (new), `src/app/(marketing)/verify/[certificateId]/page.tsx` (new), `src/app/(dashboard)/admin/certificates/page.tsx` (new), `src/config/navigation/admin.ts`, `src/app/(portal)/student/learning/[enrollmentId]/page.tsx`, `src/features/offerings/components/course-detail-view.tsx`, `public/certificates/gen-ai-certificate-preview.png`.

---

## AD-020: Course & Product Sales dashboard — a freeze exception, replaces the dead `admin/reports` placeholder rather than adding a new module to the B2B Sales CRM

**Date:** 2026-09-12
**Problem:** The founder asked for an admin dashboard tracking which students bought which courses/digital products, revenue, and per-course buyer/progress detail - "like a real company." Before writing anything, a codebase audit found: `admin/reports` was a dead, roadmap-flagged-for-deletion placeholder; "Sales CRM" in this codebase is entirely B2B (leads/quotes/projects/commission) and has never queried `Order`/`OfferingEnrollment`; `admin/students` is actually the legacy CRM `Lead` list, not real purchasers; and the CEO dashboard's "Revenue" tile is an honest "Coming soon" placeholder. No admin surface anywhere shows real course/product sales. This is a new module under AD-007's active freeze, and unlike the certificate system (AD-019), there's no already-sold customer promise behind it - it's an internal ops need, confirmed explicitly with the founder before building (this session's own AskUserQuestion exchange).
**Options considered:** 1. Hold off until the freeze lifts. 2. Build it now as a freeze exception - real sales visibility is a legitimate, if not customer-facing, operational need. 3. Extend the existing B2B Sales CRM to also cover student/course purchases. 4. Build a separate, purpose-built feature (`src/features/course-sales/`) reading `Order`/`OfferingEnrollment` directly, replacing the dead `admin/reports` placeholder instead of adding a new nav item.
**Chosen solution:** Option 2 for scope (confirmed with the founder), Option 4 for architecture.
**Reason:** Option 3 was rejected because "Sales CRM" already has a precise, established meaning in this codebase (B2B lead-to-project pipeline with its own `SalesLead`/`SalesProject`/`SalesCommission` models) - folding student/course purchases into it would blur two genuinely different domains (inbound B2B prospecting vs. direct-to-consumer checkout) that share no data model overlap today. `admin/reports` was already dead and already flagged in `ROADMAP.md` for deletion - turning it into the real feature (rather than deleting it and adding a new nav entry) keeps the nav from growing for no reason. No charting library was added - this codebase has an established "dependency-free CSS bar chart" convention (`sales-crm/components/shared/sales-bar-chart.tsx`, also used by the AI Interview platform); `ChartPoint`/`SalesBarChart` are generic enough (label/value) to import directly rather than duplicate.
**Trade-offs:** `SalesBarChart`/`ChartPoint` are now imported cross-feature from `sales-crm` into `course-sales` - a small coupling between two otherwise-independent features, accepted rather than duplicating a 30-line component or doing a bigger refactor to relocate it to a shared location neither feature was asked to touch. The per-offering detail page takes up to 200 orders unpaginated (no pagination) - fine at current volume, revisit if a single offering's buyer list ever gets large.
**Database impact:** None - no schema changes. Everything is read directly off `Order`/`OfferingEnrollment`/`Offering`/`Certificate` (existing models), via `prisma.order.groupBy`/`aggregate` (the first `groupBy`/`aggregate` calls against `Order` or `OfferingEnrollment` anywhere in this codebase - every existing aggregation query targets `SalesLead`, `Business`, or `ProposalSectionView`).
**API impact:** New `src/features/course-sales/server/queries.ts` (`getCourseSalesStats`, `getMonthlyRevenueChart`, `getOfferingSalesBreakdown`, `getRecentOrders`, `getOfferingSalesDetail`). `src/app/(dashboard)/admin/reports/page.tsx` replaced (KPIs, monthly revenue chart, per-offering revenue/enrollment table, recent orders feed); new `src/app/(dashboard)/admin/reports/[offeringId]/page.tsx` (per-offering buyer list with order status, enrollment status/progress, and certificate status). Admin nav label changed from "Reports" to "Sales" (`src/config/navigation/admin.ts`) - same route, honest label for what's actually there now.
**A real data-quality finding, not a code bug:** Building this surfaced one real `DIGITAL_PRODUCT` order (a real customer, pre-dating this session's `guest-fulfillment.ts` `isDigitalProduct` guard) that has a stray `OfferingEnrollment` row it should never have gotten. Rather than mutate that customer's data, `getOfferingSalesBreakdown`/`getOfferingSalesDetail` both derive "is this offering course-like" from the *offering's own category*, never from whether an enrollment row happens to exist - so the dashboard reads correctly regardless of that one legacy row. The stray row itself is otherwise inert (nothing else in the app reads or acts on it) and was left alone.
**Future considerations:** If a second B2B-adjacent product line (e.g. `WEBSITE_DEVELOPMENT` quote-based offerings) ever needs the same kind of "who bought what" visibility, this same query layer already covers it generically (it groups by whatever `offeringId`s actually have `PAID` orders, not a hardcoded category list) - no changes needed there. Pagination on the per-offering buyer list if volume grows.
**Related components:** `src/features/course-sales/**` (new), `src/features/sales-crm/components/shared/sales-bar-chart.tsx` and `server/dashboard-charts.ts`'s `ChartPoint` (reused), `src/app/(dashboard)/admin/reports/**`, `src/config/navigation/admin.ts`, `src/features/offerings/lib/labels.ts` (`CATEGORY_LABEL`, reused).

---

## AD-021: WhatsApp + AI Sales System foundation — centralized post-purchase service, minimal WhatsAppContact model, Cloud API integration inert until Meta credentials exist

**Date:** 2026-09-13
**Problem:** The founder shared a detailed plan for a WhatsApp-based sales funnel (post-purchase cross-sell, inbound AI-assisted conversations, agency-lead human handoff) and, after a read-only architecture audit, directed implementation of the highest-value production-ready scope in one pass. A read-only audit first (see the architecture-audit conversation turn) found two real, pre-existing bugs in the payment flow that any WhatsApp automation would inherit: guest checkout sending two separate "you paid" emails (`notifyOrderPaid`'s generic one plus `guest-fulfillment.ts`'s own richer welcome/download email), and the Razorpay webhook's non-guest PAID branch racing a concurrent `verifyPayment()` call via a non-atomic read-then-write. This is a new module under AD-007's active architecture freeze, but the founder's own request explicitly directed proceeding as a client-acquisition/revenue-infrastructure exception, not a freeze violation to flag and wait on.
**Options considered (data model for a WhatsApp contact):** A. Use `Lead` directly. B. Use `SalesLead` directly. C. A new minimal `WhatsAppContact`/`WhatsAppMessage` model, promoted into `Lead` or `SalesLead` once a conversation actually qualifies. D. Merge `Lead`/`SalesLead` into one model.
**Chosen solution:** Option C, plus a new `handleOrderPaid` centralized post-purchase service.
**Reason:** `Lead` requires a `leadType` (STUDENT/BUSINESS) decided at creation and dedups on email (nullable, but the intended key); a WhatsApp contact has neither signal until a real conversation happens. `SalesLead` requires `businessName`/`ownerName` as non-nullable - meaningless for a bare phone number. This codebase already has the exact "thin raw record → promoted into SalesLead once qualified" shape proven three times (`Business.promotedSalesLeadId`, `OfferingRequest.promotedSalesLeadId`, `Lead.promotedSalesLeadId`) - `WhatsAppContact.linkedLeadId`/`linkedSalesLeadId` (both `@unique`, mirroring that exact guarantee) is a fourth application of the same pattern, not a new mechanism. Option D was rejected outright per the founder's explicit instruction not to merge them. The centralized `handleOrderPaid` (`src/features/orders/server/post-purchase.ts`) was chosen over patching each of the 4 existing PAID call sites independently, because the duplicate-email and webhook-race bugs above are symptoms of exactly that per-call-site duplication - fixing them required one shared, atomically-idempotent entry point (`Order.postPurchaseProcessedAt`, claimed via the same conditional-`updateMany` idiom `ensureDownloadUnlocked` already uses for `downloadToken`), not four separate patches.
**Trade-offs:** `Order` gained three nullable timestamp columns (`postPurchaseProcessedAt`, `whatsappConfirmationSentAt`, `whatsappCrossSellSentAt`) rather than a separate `WhatsAppSendLog` table - simpler, and sufficient since each column has exactly one writer path; revisit only if a third independent post-purchase side effect needs its own dedup. The WhatsApp AI reply engine and Cloud API client are fully wired and tested (real Groq calls verified against the real knowledge base) but genuinely inert - `isWhatsAppSendConfigured()` returns false until real Meta credentials exist in the environment, per the founder's explicit "do not make the system appear live" instruction. The deterministic product-recommendation map (`lib/products.ts`) and agency-intent classifier (`lib/routing.ts`) are keyword/lookup-based by design, not AI-driven, per the founder's explicit "do NOT invoke AI just to make a simple decision."
**Database impact:** `Order` +3 nullable columns (see above). New `WhatsAppContact` (`whatsappId`, `phoneNumber` both `@unique`; `linkedUserId`/`linkedLeadId`/`linkedSalesLeadId`, the latter two `@unique`; `conversationState`, `optedOutAt`, `lastInboundAt`/`lastOutboundAt`) and `WhatsAppMessage` (`whatsappContactId`, `direction`, `content`, `metaMessageId @unique` - the webhook-redelivery dedup key, `aiGenerated`), plus back-relation fields on `User`/`Lead`/`SalesLead`. No existing enum widened - `LeadSource` stays untouched (a WhatsApp-promoted `Lead` uses the existing `OTHER` value plus `metadata: {channel: "whatsapp", ...}`, which the schema already provides for exactly this); `SalesLeadSource` already had a real `WHATSAPP` value, used as-is. Migration `20260913120000_whatsapp_ai_sales_system` - purely additive, no drops, applied cleanly with zero data loss.
**API impact:** New `src/features/orders/server/post-purchase.ts` (`handleOrderPaid`, the single idempotent gate `createOrder`'s FREE branch, `verifyPayment`, the Razorpay webhook, and `fulfillGuestOrder` all now route through). `notifyOrderPaid` gained an optional `{sendEmail}` param, defaulting true - `handleOrderPaid` passes `false` specifically when `order.guestEmail != null` (guest-fulfillment's own richer email already covers that purchase). `verifyPayment` and the webhook's non-guest branch both now claim the PENDING→PAID transition via `updateMany({where:{status:"PENDING"}})` instead of an unconditioned `update`. New `src/lib/whatsapp-cloud-api.ts` (lazy, server-only, same discipline as `razorpay.ts`/`groq.ts`), `src/features/whatsapp/**` (contact/promotion/reply-engine/knowledge-base/routing/rate-limit), `src/app/api/webhooks/whatsapp/route.ts` (GET verification challenge + POST handler, HMAC-verified, message-id-deduped), `src/app/api/cron/whatsapp-cross-sell/route.ts` (new Vercel Cron entry), `src/app/(dashboard)/admin/whatsapp/**` (list + conversation detail, `requireRole("ADMIN")`).
**Future considerations:** Phase 3 (full AI-driven qualification/scoring, beyond the current keyword-based agency-intent routing) was deliberately not built - the founder's own prioritized plan puts it after Phases 1-2 prove out with real conversation data. Meta template approval (order-confirmation and cross-sell templates) is the actual remaining blocker before Phase 1 sends anything for real - see the founder's final implementation report for the exact template-copy and Meta-setup checklist.
**Related components:** `prisma/schema.prisma` (`Order` columns, `WhatsAppContact`, `WhatsAppMessage`), `src/features/orders/server/post-purchase.ts` (new), `src/features/orders/server/notify.ts`, `src/features/orders/actions/order-actions.ts`, `src/features/orders/server/guest-fulfillment.ts`, `src/app/api/webhooks/payment/route.ts`, `src/lib/whatsapp-cloud-api.ts` (new), `src/features/whatsapp/**` (new), `src/app/api/webhooks/whatsapp/route.ts` (new), `src/app/api/cron/whatsapp-cross-sell/route.ts` (new), `src/app/(dashboard)/admin/whatsapp/**` (new), `src/config/navigation/admin.ts`, `.env.example`.

---

*Template for new entries — copy this block:*

```markdown
## AD-XXX: <short title>

**Date:**
**Problem:**
**Options considered:**
**Chosen solution:**
**Reason:**
**Trade-offs:**
**Database impact:**
**API impact:**
**Future considerations:**
**Related components:**
```
